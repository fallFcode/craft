import * as React from "react";
import WrapperSet from "../logic/wrapper";
import { Button } from "../ui/button";
import { Select } from "../ui/select";

interface TypeSelectButton extends React.ButtonHTMLAttributes<HTMLButtonElement> {}

export function SelectButton({ item, setSelect, ...props }: TypeSelectButton) {
  return (
    
  );
}
